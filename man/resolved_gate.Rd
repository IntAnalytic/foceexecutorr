% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/descriptor.R
\name{resolved_gate}
\alias{resolved_gate}
\title{Which selection gate chosen_model() would resolve}
\usage{
resolved_gate(descriptor)
}
\arguments{
\item{descriptor}{A \code{focex_descriptor} from \code{\link[=read_descriptor]{read_descriptor()}}, or any
list with the same shape (e.g. \code{unclass()}ed) -- accepted deliberately: a
backend's own contract (see \code{\link[=register_backend]{register_backend()}}) only promises
\code{descriptor[["covariate_search"]]} is reachable, not that \code{descriptor}
carries this package's S3 class, and a backend called directly (bypassing
\code{\link[=execute]{execute()}}, e.g. via \code{\link[=resolve_backend]{resolve_backend()}}) may be handed exactly that.
Requiring the class here would make a backend that follows this
function's own advice fail or not depending on unrelated data.}
}
\value{
\code{"covariate_search"} if that gate is present (not \code{NULL}),
otherwise \code{"structural_selection"}.
}
\description{
\code{chosen_model()} decides internally which gate to resolve
(\code{structural_selection} or \code{covariate_search}) using exactly this
question. Exported so a caller -- a custom backend deciding whether an
empty \code{covariates} field on the model it was handed means "not decided
yet" or "confirmed to have none" (see \code{\link[=register_backend]{register_backend()}}) -- can ask the
same question rather than re-implementing it, and can't fall out of sync
with it if this package's own logic ever changes.
}
\examples{
d <- read_descriptor(system.file("extdata", "model.json", package = "foceexecutorr"))
resolved_gate(d)
}
