% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/execute.R
\name{execute}
\alias{execute}
\title{Execute a signed descriptor against a backend}
\usage{
execute(descriptor, backend = "inspect", data_path = NULL, ...)
}
\arguments{
\item{descriptor}{A \code{focex_descriptor}, or a path to a descriptor file
(e.g. \verb{model-run-<run-id>.json}).}

\item{backend}{Name of a registered backend. Defaults to \code{"inspect"}, which
runs nothing -- a default that cannot be mistaken for a result.}

\item{data_path}{Optional path to the analysis-ready dataset. The descriptor
names its dataset by catalogue id, not by path, so a caller running outside
the originating system has to say where the data actually is. Always
passed to the backend, even when omitted here -- an omitted \code{data_path}
reaches \code{run()} as an explicit \code{NULL}, not as an absent argument, so a
backend wanting its own default for a missing data path must check for
\code{NULL} itself rather than relying on argument matching.}

\item{...}{Passed to the backend. May not include \code{descriptor}, \code{model}, or
\code{data_path} -- those are always the ones execute() itself resolved.}
}
\value{
An object of class \code{focex_result}.
}
\description{
The package's single entry point. Reads the descriptor if given a path, picks
the model it selected, resolves the named backend, and runs it.
}
\details{
The returned object always states which backend produced it and whether that
backend is official. That is not decoration: the reason this package exists is
that a preview estimator and a qualified one both return plausible numbers,
and once two engines can produce results there is nothing in a parameter table
that says which one you are looking at. The label is attached here, by this
function, from the backend's own registration -- never supplied by the caller.
}
\examples{
d <- system.file("extdata", "model.json", package = "foceexecutorr")
res <- execute(d)
res$official

# schema version 2 executes the same way
d2 <- system.file("extdata", "model-run-fbd67a28-06a8-40b4-9cb1-e14e6cc49ff3.json",
                   package = "foceexecutorr")
execute(d2)$result$would_run$covariates
}
